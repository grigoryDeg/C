#include <stdio.h>
int i,j,n;
int main(void) {
matrix* create_matrix(unsigned int n, unsigned int m) {
int matrix_A[N][M];
int matrix_B[][];
}

void insert_element(matrix* a, unsigned int n, unsigned int m, int x){
*(A + i*m+j)=x; 
}

void delete_element(matrix* a, unsigned int n, unsigned int m){
k = iM + j;
for (long i = k; i < n; ++i)
{
  A[i] = A[i + 1];
}
--n;
}

void transpose_matrix(matrix* a); {
	for (int i=0; i<N; ++i) {
        for (int j=0; j<N; ++j) {
            matrix_A[i][j] = matrix_B[j][i];
        }
void print_matrix(matrix* a);
for (int i=0; i<N; ++i) {
        for (int j=0; j<N; ++j) {
            printf("|%3i", matrix_A[i][j]);
        }
        printf("|\n");
    }
void free_matrix(matrix* a);

	return 0;
}